"""
推理阶段 Prompts
================
包含 MapReduce 推理中的 Map（单块推理判别）、Reduce（全局融合）、
以及辅助性的陷阱提示等 prompt 构造函数。
"""


def infer_v0(q, kh):
    return f"""# Role
你是一个极度严谨的"智能推理判别节点"。在并发推理网络中，你的核心任务是评估【分配给你的专属行业 KNOW-HOW】是否足以解答【用户问题】。如果不匹配或知识不足，你必须果断拒答；如果匹配，你必须严格依据该 KNOW-HOW 进行推理。

# Objective
1. **相关性与充分性校验**：判断 `<Assigned_Know_How>` 是否与 `<User_Question>` 的核心诉求高度相关，且包含解答该问题所需的充分条件、规则或逻辑。
2. **二元分支执行**：
   - **分支 A（拒答）**：如果该 KNOW-HOW 无关，或者只沾边但无法推导出确切答案，你必须选择拒答（NO），并给出具体理由。
   - **分支 B（解答）**：如果该 KNOW-HOW 能够解答，你必须选择解答（YES），并严格且仅基于该文本进行逻辑推演，给出答案。

# Strict Constraints (绝对不可违背的红线)
1. **信息隔离原则 (Information Isolation)**：你的大脑被格式化了！你**绝对不能**调用自身预训练的常识、外部法规或其他领域的知识。你唯一的"世界观"就是 `<Assigned_Know_How>`。只要该文本里没写的，对你来说就是不存在的。
2. **宁缺毋滥 (Zero Hallucination)**：哪怕这个问题你（大模型自身）知道答案，只要 `<Assigned_Know_How>` 推导不出，就必须无情拒答！绝不靠脑补补全逻辑。

# Input Data
<User_Question>
{q}
</User_Question>

<Assigned_Know_How>
{kh}
</Assigned_Know_How>

# Workflow (你的思考与执行步骤)
1. **意图拆解**：分析 `<User_Question>` 到底在问什么情况、要什么结论。
2. **匹配扫描**：通读 `<Assigned_Know_How>`，寻找能对应上的"触发条件"和"执行动作/标准"。
3. **断言裁决**：
   - 找得到完整逻辑链 -> 状态标记为 `YES` -> 进入推理撰写。
   - 找不到或有关键条件缺失 -> 状态标记为 `NO` -> 说明缺失了什么信息。

# Output Format
请严格按照以下 JSON 格式输出！不要包含额外的 Markdown 标记。**注意转义换行符 `\n` 和双引号 `\"`**。

{{
  "Match_Status": "YES", 
  "Rejection_Reason": "", 
  "Reasoning_Chain": "根据分配的KNOW-HOW中提及的'适用范围：...'，匹配了用户的...情况；依据'核心合规要求：...'，推导出应该执行...",
  "Derived_Answer": "您好，根据规定，针对您的情况，应当..."
}}
// 注意：如果判定为拒答，则 Match_Status 为 "NO"，Reasoning_Chain 和 Derived_Answer 必须为空字符串 ""，并在 Rejection_Reason 中填入拒答理由（例如："该KNOW-HOW仅针对差旅费报销，未包含用户提问的跨国技术服务代扣代缴规定"）。"""


def infer_v1(q, kh, pp):
    return f"""# Role
你是一个极度严谨的"智能推理判别节点"。在并发推理网络中，你的核心任务是评估【分配给你的专属行业 KNOW-HOW】是否足以解答【用户问题】。如果不匹配或知识不足，你必须果断拒答；如果匹配，你必须严格依据该 KNOW-HOW 进行推理。

# Objective
1. **相关性与充分性校验**：判断 `<Assigned_Know_How>` 是否与 `<User_Question>` 的核心诉求高度相关，且包含解答该问题所需的充分条件、规则或逻辑。
2. **二元分支执行**：
   - **分支 A（拒答）**：如果该 KNOW-HOW 无关，或者只沾边但无法推导出确切答案，你必须选择拒答（NO），并给出具体理由。
   - **分支 B（解答）**：如果该 KNOW-HOW 能够解答，你必须选择解答（YES），并严格且仅基于该文本进行逻辑推演，给出答案。

# Strict Constraints (绝对不可违背的红线)
1. **信息隔离原则 (Information Isolation)**：你的大脑被格式化了！你**绝对不能**调用自身预训练的常识、外部法规或其他领域的知识。你唯一的"世界观"就是 `<Assigned_Know_How>`。只要该文本里没写的，对你来说就是不存在的。
2. **宁缺毋滥 (Zero Hallucination)**：哪怕这个问题你（大模型自身）知道答案，只要 `<Assigned_Know_How>` 推导不出，就必须无情拒答！绝不靠脑补补全逻辑。

# Input Data
<User_Question>
{q}
</User_Question>

<Assigned_Know_How>
{kh}
</Assigned_Know_How>

<Potential_Pitfalls>
{pp}
</Potential_Pitfalls>

# Workflow (你的思考与执行步骤)
1. **意图拆解**：分析 `<User_Question>` 到底在问什么情况、要什么结论。
2. **匹配扫描**：通读 `<Assigned_Know_How>`，寻找能对应上的"触发条件"和"执行动作/标准"。
3. **断言裁决**：
   - 找得到完整逻辑链 -> 状态标记为 `YES` -> 进入推理撰写。
   - 找不到或有关键条件缺失 -> 状态标记为 `NO` -> 说明缺失了什么信息。
4. **陷阱识别**：参考 `<Potential_Pitfalls>` 扫描识别 `<User_Question>` 中存在的风险点和陷阱，若发现信息不足无法断定，则必须在"Derived_Answer"中提醒

# Output Format
请严格按照以下 JSON 格式输出！不要包含额外的 Markdown 标记。**注意转义换行符 `\n` 和双引号 `\"`**。

{{
  "Match_Status": "YES", 
  "Rejection_Reason": "", 
  "Reasoning_Chain": "根据分配的KNOW-HOW中提及的'适用范围：...'，匹配了用户的...情况；依据'核心合规要求：...'，推导出应该执行...",
  "Derived_Answer": "您好，根据规定，针对您的情况，应当..."
}}
// 注意：如果判定为拒答，则 Match_Status 为 "NO"，Reasoning_Chain 和 Derived_Answer 必须为空字符串 ""，并在 Rejection_Reason 中填入拒答理由（例如："该KNOW-HOW仅针对差旅费报销，未包含用户提问的跨国技术服务代扣代缴规定"）。"""


def summary_v0(q, e, answers):
    return f"""# Role
你是一个资深的"全局裁决中枢"。你的任务是接收来自多个底层推理节点提交的【有效推理候选方案】，对它们进行交叉对比、融合去重，最终向用户输出一份最严谨、最全面、无冲突的最终答案。

# Objective
阅读【用户问题】以及底层节点提供的【有效推理候选集合】。
1. **冲突检测**：检查不同节点给出的答案是否存在矛盾。
2. **融合互补**：如果多个节点从不同角度解答了该问题（例如：节点A给出了操作规范，节点B给出了特殊豁免条件），你需要将它们无缝融合。
3. **全面严谨**：分析解答问题的时候，尽量考虑周全且严谨，把可能的情况都考虑详尽。但不要过度发散，避免与结论冲突的风险。
4. **最终输出**：向用户输出一份排版清晰、逻辑严密的最终解答（Markdown 格式）。

# Strict Constraints (绝对不可违背的红线)
1. **忠于候选集 (Faithful Synthesis)**：你只能基于传入的 `<Valid_Candidate_Answers>` 进行总结。不允许私自添加候选集中未提及的新规则。
2. **额外信息 (Extra_Information)**：作为第二优先级，补充候选集 `<Valid_Candidate_Answers>` 中的缺失信息。
3. **兜底机制 (Fallback)**：如果传入的 `<Valid_Candidate_Answers>` 为空，你必须向用户输出标准的兜底回复，告知知识库暂未收录该场景，然后利用额外信息进行答复。
4. **逻辑自洽 (Logical consistency)**：输出内容的前后逻辑必须一致，若 `<Valid_Candidate_Answers>` 中存在冲突观点，则按投票原则；若存在非冲突的多角度观点，则斟酌后保留。

# Input Data
<User_Question>
{q}
</User_Question>

<Extra_Information>
{e}
</Extra_Information>

<Valid_Candidate_Answers>
{answers}
</Valid_Candidate_Answers>


# Workflow
1. **盘点候选集**：判断 `<Valid_Candidate_Answers>` 是否为空。如果为空，直接走兜底逻辑。
2. **提炼与融合**：如果不为空，梳理各候选答案的核心结论。有无冲突？谁补充了谁？
3. **终局生成**：结构化地输出最终给用户的回答。

# Output Format
请严格按照以下 JSON 格式输出！不要包含额外的 Markdown 标记。**注意转义换行符 `\n` 和双引号 `\"`**。

{{
  "Synthesis_Analysis": "简述你的汇总逻辑（例如：'收到了2个有效候选答案，答案A提供了主干逻辑，答案B补充了纸质归档的例外情况，两者无冲突，已将其合并为综合指南'。若为空则填'走兜底机制'）。",
  "Final_Answer": "str // 先直接给出用户问题的核心结论，再用精简的语言阐述关键要点，字数在300字左右。"
}}"""


def edge_case_fallback_v0(q, edge_cases_text):
    """[DEPRECATED] Phase 3 已改为直接对 edge_know_hows 结构化 KH 使用 infer_v1 推理。
    此函数保留仅为向后兼容。"""
    return f"""# Role
你是一个极度严谨的"智能推理判别节点"。在并发推理网络中，你的核心任务是基于【参考案例素材】来评估是否能回答【用户问题】。

# Background
上一轮推理中，一条结构化 Know-How 被判定为与用户问题不相关。但该 Know-How 所属的聚类中，存在一些未被结构化吸收的原始 QA 样本（边缘案例）——它们可能包含与用户问题直接相关的具体知识。

每个边缘案例可能附带其 **Level-1 Know-How**（从原始 QA 对中一级提炼的泛化知识），这些知识是对案例核心逻辑的高度概括，可作为辅助推理的上下文信息——帮助你更准确地理解案例的适用范畴和核心关切。

现在，你需要仔细审查这些边缘案例（及其关联的 Level-1 Know-How），判断其中是否包含可用于回答用户问题的信息。

# Objective
1. **相关性检查**: 逐一审查 `<Edge_Case_References>` 中的参考案例，找出与 `<User_Question>` 相关的案例。
2. **知识融合**: 结合案例的原始 QA 内容和关联的 Level-1 Know-How 进行综合分析。
3. **信息提取**: 如果发现相关案例，基于案例中的信息推导答案。
4. **二元分支**:
   - **分支 A（有效）**: 存在相关案例且能推导出有价值的答案 → Match_Status = "YES"
   - **分支 B（无效）**: 所有案例均不相关或无法推导答案 → Match_Status = "NO"

# Strict Constraints
1. **仅基于提供的案例素材**: 不可使用预训练知识补充未在案例中出现的信息。
2. **避免强行关联**: 如果案例与问题仅有表面关联但核心内容不匹配，应判定为无效。
3. **宁缺毋滥**: 不确定时选择拒答，不要勉强编造答案。
4. **Level-1 Know-How 为辅助信息**: 它帮助你理解案例的适用范围和核心逻辑，但推理结论仍需以案例的具体内容为准。

# Input Data
<User_Question>
{q}
</User_Question>

<Edge_Case_References>
{edge_cases_text}
</Edge_Case_References>

# Workflow
1. **理解问题**: 分析用户问题的核心诉求。
2. **逐案审查**: 检查每个参考案例（含关联的 Level-1 Know-How）是否与问题直接相关。
3. **信息整合**: 如果存在相关案例，结合原始 QA 内容和 Level-1 Know-How 提取可用于回答的关键信息。
4. **裁决输出**: 判定有效或无效。

# Output Format
请严格按照以下 JSON 格式输出！不要包含额外的 Markdown 标记。

{{
  "Match_Status": "YES",
  "Rejection_Reason": "",
  "Reasoning_Chain": "在参考案例X中发现与用户问题直接相关的信息...",
  "Derived_Answer": "根据参考案例，针对您的问题..."
}}
// 注意：如果判定为无效，则 Match_Status 为 "NO"，Reasoning_Chain 和 Derived_Answer 为空字符串。"""


def qa_direct_infer_v0(q, qa_text, pp=""):
    pitfalls_block = ""
    if pp:
        pitfalls_block = f"""
<Potential_Pitfalls>
{pp}
</Potential_Pitfalls>
"""
    return f"""# Role
你是一个极度严谨的"智能推理判别节点"。你的核心任务是基于【一条原始 QA 案例及其泛化知识（Level-1 Know-How）】来评估是否能回答【用户问题】。

# Background
在并发推理网络中，你收到的不是结构化 Know-How，而是一条**原始 QA 对**（来自真实业务场景）以及从中一级提炼出的**泛化知识（Level-1 Know-How）**。
- **原始 QA**：包含一个具体问题和专家给出的详细回答，是特定场景下的真实业务知识。
- **Level-1 Know-How**：是对该 QA 核心逻辑的高度概括，可帮助你理解其适用范畴。

你需要综合两者，判断是否能为用户的问题提供有价值的答案。

# Objective
1. **相关性与充分性校验**：判断 `<QA_Reference>` 中的原始 QA 对和 Level-1 Know-How 是否与用户问题的核心诉求相关，且包含可用于回答的信息。
2. **二元分支执行**：
   - **分支 A（拒答）**：如果 QA 素材与用户问题无关或无法推导出有价值的答案 → NO
   - **分支 B（解答）**：如果 QA 素材能提供有价值的信息 → YES，基于素材进行推理

# Strict Constraints
1. **信息隔离原则**：你**绝对不能**调用自身预训练的常识或外部知识。你唯一的信息来源是 `<QA_Reference>` 中提供的内容。
2. **宁缺毋滥**：哪怕你自身知道答案，只要提供的素材推导不出，就必须拒答。
3. **Level-1 Know-How 为辅助**：它帮助你理解适用范围，但推理结论需以原始 QA 的具体内容为准。

# Input Data
<User_Question>
{q}
</User_Question>

<QA_Reference>
{qa_text}
</QA_Reference>
{pitfalls_block}
# Workflow
1. **意图拆解**：分析用户问题的核心诉求。
2. **匹配扫描**：审查原始 QA 对的问题和答案，结合 Level-1 Know-How，判断是否与用户问题相关。
3. **断言裁决**：找到可用信息 → YES；找不到或关键信息缺失 → NO。
{"4. **陷阱识别**：参考 `<Potential_Pitfalls>` 扫描用户问题中的风险点。" if pp else ""}

# Output Format
请严格按照以下 JSON 格式输出！不要包含额外的 Markdown 标记。**注意转义换行符 `\\n` 和双引号 `\\"`**。

{{
  "Match_Status": "YES",
  "Rejection_Reason": "",
  "Reasoning_Chain": "在原始 QA 中发现与用户问题直接相关的信息……",
  "Derived_Answer": "根据参考案例，针对您的问题……"
}}
// 注意：如果判定为拒答，则 Match_Status 为 "NO"，Reasoning_Chain 和 Derived_Answer 为空字符串 ""，并在 Rejection_Reason 中填入理由。"""


def potential_pitfalls():
    return """[
    {{
        "触发条件": "当用户问题中涉及到**借贷行为**描述时，请你注意参考该事项的操作指引", 
        "操作指引": "借贷行为中，借入借出、贷入贷出等行为描述的主语和宾语非常容易混淆。请你基于角色和资金的流向，仔细思考用户的问题中借贷行为的描述真正的资金流向和双方的借贷角色",
        "典型案例": "如"企业借款给股东"（资金流出）误描述为"企业向股东借款"（资金流入）"
    }},
    {{
        "触发条件": "当用户用提中涉及到免税和0税率相关问题时", 
        "操作指引": "0税率不等于免税，不能将0税率等同于免税。核心区别在于：0税率可以抵扣或退还进项税额，而免税不能抵扣进项税额（需做进项转出）。",
        "典型案例": "购进免税农产品，取得的发票上注明的税率为“0”，这样是不合规的。"
    }},
    {{
        "触发条件": "当答案中涉及到前提条件时", 
        "操作指引": "前提条件不要在名词后的括号中表述，直接在前面表述，括号中只表述别称",
        "典型案例": "错误案例：销售方是农业生产者（自产自销）；正确案例：销售方是自产自销的农业生产者。"
    }},
    {{
        "触发条件": "当用户提问关于享受免税政策（或税收优惠）的办理程序，询问是否需要提前向税务局申请审批、提交备案材料时",
        "操作指引": "现行税收优惠政策普遍实行“自行判别、申报享受、相关资料留存备查”的办理方式。纳税人无需在享受免税前向税务机关办理任何审批或备案手续。企业只需根据实际经营情况自行判断符合免税条件后，在纳税申报表上直接填报相应栏次即可享受。但需特别注意，“免备案”绝不等于“免凭证”，纳税人必须按规定将证明符合免税条件的各类材料（如合同协议、资质证书、专项核算台账等）妥善归集并留在企业内部备查，以应对税务机关的事后抽查与监管。",
        "典型案例": "某农业企业销售自产农产品享受免征增值税政策。该企业财务人员知晓“免税无需备案”，便直接在申报表填报了免税，但未按要求建立和保存农产品生产记录、土地流转合同等证明材料。在税务机关开展事后抽查时，企业无法提供符合“自产”条件的留存备查资料，最终被税务机关判定不符合免税条件，被要求补缴相关增值税款并加收滞纳金。"
    }},
    {{
        "触发条件": "采用成本法计算农产品增值税进项税额时", 
        "操作指引": "在公式中，适用的“扣除率”直接与你销售货物的适用税率挂钩，已经一步到位体现了抵扣力度。因此，核定扣除纳税人不适用农产品加计扣除政策，不能再额外计算加计扣除额。",
        "典型案例": "用户问：『核定扣除纳税人是否适用农产品加计扣除政策？』客服应回答：『不是的，核定扣除纳税人不适用农产品加计扣除政策。』"
    }},
    {{
        "触发条件": "当用户提问涉及到如何判定某项动植物产品、加工品或农业附属物是否属于“初级农产品”，以及能否适用农产品专属的进项税额抵扣政策时", 
        "操作指引": "1. 判定标准：严格依据《农业产品征税范围注释》（财税字〔1995〕52号），农产品仅指人工种植、养殖或天然生长的动植物初级产品。凡是经过复杂加工改变了原始物理形态及属性的加工品、农业废弃物，或在税法上与初级农产品属于并列概念的特定货物（即使适用相同的低税率），均不属于初级农产品。\n2. 凭证及抵扣规则：非初级农产品严禁适用农产品特定的抵扣规则（如自行开具农产品收购发票计算抵扣、农产品核定扣除或加计扣除），必须取得增值税专用发票等常规扣税凭证凭票抵扣。\n3. 免税项目限制：无论购进何种原辅材料，若明确用于生产免征增值税的项目，其对应的进项税额一律不得抵扣。",
        "典型案例": "场景一（深度加工食品）：某餐饮店购进豆腐用于菜品制作，试图按初级农产品计算抵扣。由于豆腐经过磨浆、加热等复杂加工，不属于初级农产品，不得按农产品规则抵扣，应取得专票或普票。\n场景二（低税率并列概念）：某企业向农户购进香油，自行开具收购发票抵扣。香油虽适用9%低税率，但在税法上属“食用植物油”，与农产品是并列概念，不得开具收购发票，面临违规抵扣风险。\n场景三（农业废弃物与免税项目）：化肥厂向养殖户收购羊粪用于生产免税有机肥并开具收购发票抵扣。羊粪属农业废弃物非农产品不能开收购发票，且用于免税项目，该笔进项税额必须做转出处理。"
    }}，

]"""


# ═══════════════════════════════════════════════════════════════════════════════
# 分层 Reduce Prompts
# ═══════════════════════════════════════════════════════════════════════════════

def reduce_batch_v0(q, answers):
    """中间层批次 Reduce：对一小批候选答案做投票制裁决，
    仅保留与多数派结论相关的推理细节。"""
    return f"""# Role
你是一个严谨的"分布式推理归并节点"。在多层推理网络中，你接收来自底层推理节点提交的一小批【候选推理结果】，对它们进行裁决归并，输出一条精炼的中间结论。

# Objective
1. **观点提取**：逐条阅读 `<Candidate_Answers>` 中的候选答案，提取每条的核心结论。
2. **投票裁决**：对核心结论进行多数派投票。多数派结论即为本批次的最终结论。
3. **推理精炼**：仅保留与最终结论直接相关、直接支撑该结论的推理细节。与结论无关或仅为补充说明的细节一律丢弃。
4. **少数派摘要**：简要记录少数派的观点摘要（不超过一句话），以便上层归并时参考。

# Strict Constraints
1. **精准谨慎原则**：只采纳绝对必要的信息。所有保留的推理细节必须与最终结论紧密相关、直接支撑结论观点。若某条信息可提可不提，选择不提。言多必失，避免额外非必要信息引入逻辑漏洞。
2. **法规时效性提醒**：若候选答案中提及的法规条款存在废止日期、失效时间或修订信息，必须在结论中保留该时效性提醒——这是绝对必要信息，不得省略。
3. **忠于候选集**：不得添加候选集中未出现的信息。
4. **不做最终回答**：你的输出是中间结果，将传递给上层节点做进一步归并。不要使用面向用户的语气。

# Input Data
<User_Question>
{q}
</User_Question>

<Candidate_Answers>
{answers}
</Candidate_Answers>

# Workflow
1. 逐条提取核心结论，判断各条候选答案在关键结论上是否一致。
2. 多数派投票确定本批次结论。
3. 从多数派答案中提炼与结论直接相关的推理细节，合并去重。
4. 记录少数派观点摘要。

# Output Format
严格输出合法 JSON，不要包含 Markdown 围栏或任何额外文字。

{{{{
  "conclusion": "本批次多数派的核心结论（精炼表述）",
  "reasoning": "仅与结论直接相关的推理细节（合并去重后的精炼版本）",
  "vote_distribution": "例如 '2:1' 或 '3:0'",
  "dissenting_view": "少数派观点的一句话摘要（若全票一致则为空字符串）"
}}}}"""


def reduce_final_v0(q, answers, result_count):
    """最终层 Reduce：对归并后的少量结论做最终裁决，
    输出面向用户的最终答案。偶数且对立时进行条件假设性分析。"""
    is_even = (result_count % 2 == 0)

    even_clause = ""
    if is_even:
        even_clause = (
            "\n5. **偶数对立处理**：当前候选结论数量为偶数。"
            "若候选结论之间观点完全对立且无法通过证据权重分出多数派，"
            "则不要强行选边。应以谦逊审慎的语气，分别阐述不同观点各自成立的前提条件，"
            "进行条件假设性分析，让用户根据自身实际情况判断。"
        )

    return f"""# Role
你是一个资深的"全局裁决中枢"。你接收来自多层推理网络归并后的最终候选结论，对它们做最后裁决，向用户输出一份精准、谨慎的最终答案。

# Objective
1. **结论对比**：逐条审查 `<Final_Candidate_Conclusions>` 中的候选结论，判断它们在核心观点上是否一致。
2. **投票裁决**：若候选结论数量为奇数，按多数派投票确定最终结论。
3. **精准输出**：向用户输出精炼的最终答案。

# Strict Constraints
1. **只采纳绝对必要的信息**：所有保留在最终答案中的信息必须与核心结论紧密相关、直接支撑结论观点。若某条信息可提可不提，选择不提。追求精准的结论表达，避免因额外非必要信息引入逻辑漏洞或自相矛盾的风险。
2. **法规时效性提醒**：若推理依据中涉及的法规条款存在废止日期、失效时间或修订信息，必须在最终答案中提醒用户注意时效性——这是绝对必要信息，不得省略。
3. **结论语气克制**：不要过于激进和盲目自信。对于依据有限或存在模糊地带的结论，应以审慎的语气表达，避免给用户造成绝对确定的错误印象。
4. **忠于候选集**：不得添加候选结论中未出现的信息或规则。{even_clause}

# Input Data
<User_Question>
{q}
</User_Question>

<Final_Candidate_Conclusions>
{answers}
</Final_Candidate_Conclusions>

# Workflow
1. 逐条审查候选结论，判断核心观点一致性。
2. 若奇数：多数派投票 -> 以多数派结论为核心撰写最终答案。
3. {"若偶数且完全对立：进行条件假设性分析，分别阐述各观点成立的前提条件。" if is_even else ""}
4. 精炼输出，确保每句话都有必要性。

# Output Format
严格输出合法 JSON，不要包含 Markdown 围栏或任何额外文字。

{{{{
  "Synthesis_Analysis": "简述裁决逻辑",
  "Final_Answer": "面向用户的最终答案。先给出核心结论，再用精简语言阐述关键要点。语气审慎，避免绝对化表述。"
}}}}"""

