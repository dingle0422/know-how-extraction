import re
import json
import json5 as _json5


def safe_parse_json(text: str) -> dict:
    """
    多策略 JSON 解析，专为大模型输出设计。

    策略顺序：
    1. json5 直接解析
    2. 剥离 Markdown 代码围栏（```json … ```）后解析
    3. 用状态机修复字符串内未转义的换行/制表符后解析
    4. 正则提取最外层 {...} 块，再执行策略 3
    """

    def _fix_unescaped_in_strings(s: str) -> str:
        """状态机遍历，将 JSON 字符串值内的裸换行、制表符替换为合法转义序列。"""
        result = []
        in_string = False
        escape_next = False
        for ch in s:
            if escape_next:
                result.append(ch)
                escape_next = False
            elif ch == '\\' and in_string:
                result.append(ch)
                escape_next = True
            elif ch == '"':
                in_string = not in_string
                result.append(ch)
            elif in_string and ch == '\n':
                result.append('\\n')
            elif in_string and ch == '\r':
                result.append('\\r')
            elif in_string and ch == '\t':
                result.append('\\t')
            else:
                result.append(ch)
        return ''.join(result)

    # 策略 1：直接解析
    try:
        return _json5.loads(text)
    except Exception:
        pass

    # 策略 2：剥离 Markdown 围栏
    stripped = re.sub(r'^```(?:json)?\s*', '', text.strip(), flags=re.MULTILINE)
    stripped = re.sub(r'\s*```\s*$', '', stripped.strip(), flags=re.MULTILINE)
    stripped = stripped.strip()
    try:
        return _json5.loads(stripped)
    except Exception:
        pass

    # 策略 3：修复裸换行后解析
    try:
        return _json5.loads(_fix_unescaped_in_strings(stripped))
    except Exception:
        pass

    # 策略 4：正则提取 {...} 最外层块，再修复裸换行
    m = re.search(r'\{[\s\S]*\}', stripped)
    if m:
        try:
            return _json5.loads(_fix_unescaped_in_strings(m.group(0)))
        except Exception:
            pass

    raise ValueError(
        f"safe_parse_json：所有解析策略均失败。原始内容前 300 字符：\n{text[:300]}"
    )


experts_prompt_l1 = """请依据问答内容“问题+答案”，拟人化呈现财税专家解决该问题的完整思维过程，供模型学习。开头精准点明用户核心关切与问题实质，再按内容智能区分处理：仅会计问题只推演会计端，仅税务问题只推演税务端，既有会计又有税务则同步推演。会计端：围绕业务实质，涉及准则直接引用并展示应用逻辑；模糊或新兴场景，结合行业惯例、职业判断，剖析处理方式的影响与风险，推导账务处理与会计分录。税务端：答案中有明确政策就锚定引用《政策文件名称》(文号) 条款并展示应用逻辑，无对应政策则按业务实质、合规口径推导最优处理与风险防控方案。整体自然连贯、环环相扣，无小标题、答案复述、总结句与固定模板，重在展现真实、鲜活、有逻辑的推理路径，字数控制在 300 字以内（最高不超过 500 字），杜绝末尾总结陈述或结果印证。"""
   

def single_v0(eb = None, q = None, r = None, a = None, kh = None):
    return f"""# Role
你是一个资深的行业知识工程师与智能体系统的“规则泛化与提炼中枢”。你的核心任务是在并发处理流程中，精准扫描现有知识库的盲区，并**透过具体的业务样本，提炼出具有普适性、泛化性、指导性的行业标准 KNOW-HOW**，最后将其结构化为 Markdown 格式。

# Objective
阅读【原有 KNOW-HOW】，尝试仅凭借它去推导【标准答案】以解答【用户问题】。
- 如果原逻辑能够完美“自圆其说”，说明该知识点已被覆盖，你无需做任何事。
- 如果原逻辑卡壳、断层或为空（无法自圆其说），你必须将缺失的业务逻辑**进行向上抽象与泛化提取**，并写成一个独立的 **Markdown 格式片段**，供下游系统合并。

# Compression
**非不要不添加新的know-how，而应该去做以下3点操作**
1. **去重与合并**：识别出那些针对同一业务场景但表述不同的碎片知识，将它们合并。
2. **升维与总结**：不要将片段进行简单的机械堆砌！必须将相似的场景向上抽象为“通用业务准则”或“全局合规要求”。
3. **结构化重组**：输出一份全新的、排版极其专业的 Markdown 格式 KNOW-HOW 手册。

# Strict Constraints (绝对不可违背的红线)
1. **苛刻的自洽校验 (Strict Self-Consistency)**：不要替 `<Original_Know_How>` 脑补！如果原文本中没有明确包含所需的判断条件、通用规则或政策依据，就判定为“无法自圆其说”。如果 `<Original_Know_How>` 为“暂无”或空，也必定判定为“无法自圆其说”。
2. **按需静默输出 (Silent on Success)【最高指令】**：当且仅当原 KNOW-HOW 能够完整、严密地推导出 `<Standard_Answer>` 时，输出字段 `"Know_How"` 的值必须是一个**绝对的空字符串 `""`**。
3. **泛化提炼原则 (Generalization Principle)**：当判定无法自圆其说时，`"Know_How"` 字段输出的增量逻辑**必须避免“就事论事”**。你需要剔除用户提问中的个性化细节，提炼出适用于该类业务的**通用规范、底层准则或标准SOP**（明确“适用范畴”、“通用合规要求”、“特殊豁免条件”等），并**绝对不要重写或重复原有的其他逻辑**。
4. **绝对尊重原文 （Subject to Input Data）**：所有泛化提炼内容，必须是 `<Standard_Answer>` 和 `<Reasoning_Chain>` 中提及的标准或与事实高度相关的行业标准，否则宁可不要

# Input Data
<User_Behavior>
{eb}
</User_Behavior>

<User_Question>
{q}
</User_Question>

<Reasoning_Chain>
{r}
</Reasoning_Chain>

<Standard_Answer>
{a}
</Standard_Answer>

<Original_Know_How>
{kh}
</Original_Know_How>

# Workflow (你的思考与执行步骤)
请在脑海中严格按照以下步骤推演，但最终只输出要求的 JSON：
1. **闭卷推导 (Closed-book Deduction)**：只看 `<Original_Know_How>` 和 `<User_Question>`。原文本里有没有涵盖应对此问题的充分条件和正确动作？
2. **断点判定 (Breakpoint Check)**：
   - 场景 A：原文本完美覆盖。准备将 `Know_How` 置空。
   - 场景 B：原文本为空，或缺少相关业务准则。准备提取增量 `Know_How`。
3. **泛化提炼 (Generalized Markdown Generation)**：针对场景 B，根据 `<Reasoning_Chain>`，**不要复述原问题的特定细节，而是要向上抽象一层**，提炼出具有普适性的行业规则，转化为逻辑严密、结构清晰的 Markdown 结构片段。

# Output Format
请严格按照以下 JSON 格式输出！不要包含任何额外的 Markdown 标记（如 ```json）。
**⚠️ 极其重要的转义警告**：如果需要输出 Markdown 片段，必须将 Markdown 文本中的所有换行符替换为 `\n`，所有双引号替换为 `\"`，以确保最终输出的是合法的单层 JSON！

{{
  "Logic_Diagnosis": "详细说明推演过程。例如：'原KNOW-HOW为空，需根据该样本提取关于电子会计凭证无纸化归档的通用准则' 或 'Original_Know_How可完美推理出Standard_Answer，无需新增'。",
  "Know_How": "### Know-How概括性标题 \n- **核心思维链**： 请依据问答内容 `<User_Question>` 和 `<Standard_Answer>`，拟人化呈现财税专家解决该问题的完整思维过程，供模型学习。开头精准点明用户核心关切与问题实质，再按内容智能区分处理：仅会计问题只推演会计端，仅税务问题只推演税务端，既有会计又有税务则同步推演。会计端：围绕业务实质，涉及准则直接引用并展示应用逻辑；模糊或新兴场景，结合行业惯例、职业判断，剖析处理方式的影响与风险，推导账务处理与会计分录。税务端：答案中有明确政策就锚定引用《政策文件名称》(文号) 条款并展示应用逻辑，无对应政策则按业务实质、合规口径推导最优处理与风险防控方案。整体自然连贯、环环相扣，无小标题、答案复述、总结句与固定模板，重在展现真实、鲜活、有逻辑的推理路径，字数控制在 300 字以内（最高不超过 500 字），杜绝末尾总结陈述或结果印证。\n- **适用范畴**：若`<Standard_Answer>`提及，则需抽取。\n- **政策依据**：若`<Standard_Answer>`中提及，则需抽取" 
}}
// 注意：如果 Logic_Diagnosis 判定可以自圆其说，则 "Know_How": ""
"""


def single_v1(eb = None, q = None, r = None, a = None):
    return f"""# Role
你是一台独立运行的「行业知识提炼引擎」，专精于从单一业务样本中**向上抽象**出具备普适性、可复用性的行业标准 KNOW-HOW。你的输出将直接进入并发的知识库构建流水线，与其他抽取结果进行最终归并。

# Objective
基于输入的业务样本（用户行为、问题、推理链、标准答案），**独立执行一次完整的知识泛化与提炼**，生成结构化的 Markdown 格式 KNOW-HOW 片段。
- **核心任务**：剔除样本中的个性化细节，提炼出适用于该类业务场景的通用规范、底层准则或标准SOP。
- **去重策略**：无需关心其他并发节点正在抽取什么内容，专注于将当前样本提炼到最精炼、最泛化的形态，便于后续的自动去重与合并。

# Compression (质量优化指令)
在生成本次 KNOW-HOW 前，必须执行以下质量优化：

1. **升维抽象**：绝对禁止"就事论事"或简单复述标准答案。必须向上抽象一层，提炼出**通用业务准则**或**全局合规要求**。
   - ❌ 差示例："用户询问数电票如何归档，答案是保存XML格式"
   - ✅ 优示例："电子会计凭证无纸化归档的通用准则：归档文件必须包含完整数字签名的原始文件（如XML），满足条件可豁免纸质存档"

2. **结构化重组**：使用 Markdown 标题层级清晰定义知识边界。
   - 必须包含：适用范畴（Scope）、核心规则（Rules）、豁免/例外（Exceptions）、政策/依据（References，仅当标准答案明确提及时才写）

3. **原子化剥离**：确保本次抽取的 KNOW-HOW 是一个**独立的、自洽的知识单元**，不依赖于其他片段即可理解。

# Strict Constraints (抽取红线)
1. **绝对泛化原则**：`Know_How` 内容必须剔除所有当前样本的个性化数据（如具体金额、特定企业名称、个案时间等），仅保留可复用的业务逻辑。
2. **原文依从性**：所有规则必须源自 `<Standard_Answer>;` 和 `<Reasoning_Chain>;` 中明确提及的标准或事实，**严禁脑补**标准答案中未提及的政策依据。
3. **格式纯净度**：输出内容必须是可直接入库的知识片段，不含任何解释性前言（如"根据用户问题..."）或结语。
4. **零重复**：如果 `<Standard_Answer>;` 和 `<Reasoning_Chain>;` 仅为简单事实陈述且无法提炼出通用规则（如纯数值计算、特定日期查询），则判定为"无通用知识可抽取"，输出空字符串。

# Input Data
<User_Behavior>;
{eb}
</User_Behavior>;

<User_Question>;
{q}
</User_Question>;

<Reasoning_Chain>;
{r}
</Reasoning_Chain>;

<Standard_Answer>;
{a}
</Standard_Answer>;

# Workflow (执行步骤)
请在脑海中完成以下推演，最终输出要求的 JSON：

1. **样本定性**：分析 `<Standard_Answer>;` 和 `<Reasoning_Chain>;` —— 这是否包含可泛化的业务规则、合规要求或标准SOP？还是仅为针对特定事实的个性化回答？
   - 如果是后者（如查询某条特定法规的具体条款号、计算特定数值），标记为"无泛化价值"，准备输出空。
   - 如果是前者（包含判断逻辑、处理流程、合规标准），进入步骤2。

2. **要素解构**：提取标准答案中的核心逻辑要素：
   - 适用条件（什么场景下适用？）
   - 通用规则（必须做什么/禁止做什么？）
   - 豁免条件（什么情况下可以例外？）
   - 合规依据（基于什么标准/法规？）

3. **泛化重构**：将要素转化为行业通用表述：
   - 将具体业务对象抽象为类别（如"数电票"→"电子会计凭证"）
   - 将具体操作抽象为准则（如"保存XML"→"归档原始电子文件"）

4. **Markdown结构化**：使用专业排版生成最终片段。

# Output Format
严格按照以下 JSON 格式输出。不要包含任何额外的 Markdown 标记（如 ```json）。
**⚠️ 转义要求**：将 Markdown 文本中的所有换行符替换为 `\n`，双引号替换为 `\"`，确保合法 JSON。

{{
  "Logic_Diagnosis": "说明本次提炼逻辑。例如：'该样本涉及电子凭证归档规则，已泛化为通用无纸化归档准则' 或 '该样本为特定数值查询，无通用业务规则可抽取'。",
  "Know_How": "### Know-How概括性标题 \n- **核心思维链**： 请依据问答内容 `<User_Question>` 和 `<Standard_Answer>`，拟人化呈现财税专家解决该问题的完整思维过程，供模型学习。开头精准点明用户核心关切与问题实质，再按内容智能区分处理：仅会计问题只推演会计端，仅税务问题只推演税务端，既有会计又有税务则同步推演。会计端：围绕业务实质，涉及准则直接引用并展示应用逻辑；模糊或新兴场景，结合行业惯例、职业判断，剖析处理方式的影响与风险，推导账务处理与会计分录。税务端：答案中有明确政策就锚定引用《政策文件名称》(文号) 条款并展示应用逻辑，无对应政策则按业务实质、合规口径推导最优处理与风险防控方案。整体自然连贯、环环相扣，无小标题、答案复述、总结句与固定模板，重在展现真实、鲜活、有逻辑的推理路径，字数控制在 300 字以内（最高不超过 500 字），杜绝末尾总结陈述或结果印证。\n- **适用范畴**：若`<Standard_Answer>`提及，则需抽取。\n- **政策依据**：若`<Standard_Answer>`中提及，则需抽取" 
}}
// 注意：当 Logic_Diagnosis 判定为"无泛化价值"时，"Know_How" 必须为 """""


def compression_v0(f = None):
    return f"""# Role
你是一个资深的行业知识体系架构师与“知识库主编”。你的核心任务是对并发收集上来的众多【碎片化增量知识】进行全局去重、高度概括、分类提炼，并将它们内化融合成一份结构严密、具有高度概括性的【全局标准 KNOW-HOW】。

# Objective
阅读【原有 KNOW-HOW】以及各个节点提取上来的【碎片化增量片段】。
1. **去重与合并**：识别出那些针对同一业务场景但表述不同的碎片知识，将它们合并。
2. **升维与总结**：不要将片段进行简单的机械堆砌！必须将相似的场景向上抽象为“通用业务准则”或“全局合规要求”。
3. **结构化重组**：输出一份全新的、排版极其专业的 Markdown 格式 KNOW-HOW 手册。

# Strict Constraints (绝对不可违背的红线)
1. **禁止机械拼接 (No Copy-Pasting)**：绝不允许把输入的片段原封不动地排成一个长列表。你必须咀嚼这些知识，提炼出它们的共性逻辑（例如：将多个关于发票归档的特殊情况，总结成一个包含“基本原则”和“例外清单”的综合归档准则）。
2. **结构化标准 (Strict Markdown Hierarchy)**：最终输出的文本必须具备教科书般的结构，必须包含且不限于：
   - `# 宏观业务准则 (核心原则总结)`
   - `## 关键定性要素 (判断该类业务的关键特征)`
   - `## 标准操作规范 SOP (核心的执行要求)`
   - `## 特殊场景与例外 (将特例分类罗列)`
   - `## 政策与合规依据汇总`
3. **知识无损原则 (Lossless Synthesis)**：在高度概括的同时，绝对不能丢失原有 KNOW-HOW 和增量片段中具体的判断阈值、前提条件或政策文件号。

# Input Data
<Fragmented_Delta_Snippets>
{f}
</Fragmented_Delta_Snippets>

# Workflow (你的思考与执行步骤)
1. **知识盘点**：通读原有知识与所有碎片片段，归纳出它们共同指向的 1~3 个核心业务主题。
2. **逻辑提炼**：找出这些片段中的“共性原则”（提取为宏观准则）和“个性差异”（提取为特殊场景/例外分支）。
3. **重构撰写**：以“行业专家撰写SOP指南”的口吻，使用 Markdown 格式重新组织全文，确保前后文语气统一、逻辑连贯、高度概括。

# Output Format
请严格按照以下 JSON 格式输出！不要包含任何额外的 Markdown 标记（如 ```json）。
**⚠️ 极其重要的转义警告**：必须将生成的 Markdown 文本中的所有换行符替换为 `\n`，所有双引号替换为 `\"`，以确保最终输出的是合法的单层 JSON！

{{
  "Synthesis_Summary": "简要说明你进行了哪些合并与概括（例如：'将3个关于数电票不同情况的片段归纳成了《电子凭证无纸化归档综合规范》，统一了前置条件'）。",
  "Final_Know_How": "# 电子会计凭证无纸化归档综合规范\n\n## 一、 宏观业务准则\n企业取得电子形式的会计凭证（含数电票等），在满足合法性与防伪特征完整性的前提下，应优先采用无纸化归档……\n\n## 二、 关键定性要素\n- **要素1**：必须包含数字签名（XML格式）。\n- **要素2**：业务系统需具备电子档案管理能力。\n\n## 三、 特殊场景与例外\n- **特例A**：若确实需要纸质打印，则必须同时保存电子源文件……\n\n## 四、 政策依据\n《财会[2020]6号》、《档办发〔2023〕2号》" 
}}
"""


def shrink_v0(oversized_kh: str, target_chars: int = 5000) -> str:
    return f"""# Role
你是一位顶级的「知识库精炼编辑」。你的唯一任务是：在**绝对不丢失任何关键业务规则**的前提下，将一份过长的 KNOW-HOW 文档**压缩到约 {target_chars} 字以内**。

# Objective
对输入的【超长知识库】执行"有损压缩"，通过删除冗余表达、合并重复段落、精简示例描述，使输出长度控制在目标范围内，同时保留全部核心知识骨架。

# 压缩策略（按优先级排序）
1. **删除冗余示例**：如果某条规则后面有"例如……"的举例说明，且该例子只是对规则的重复，直接删除举例。
2. **合并同类条目**：将描述同一核心规则但措辞略有差异的子条目合并为一条精炼表述。
3. **精简过渡语言**：删除章节间的过渡段落、"总体而言"等不含规则的修辞句。
4. **压缩政策依据**：只保留法规名称和文号，删除法规的具体条文内容描述。
5. **保留标题骨架**：所有 Markdown 标题（#/##/###）必须完整保留，不得删除任何章节标题。

# 绝对禁止删除的内容（红线）
- 所有具体数值阈值（如"≤100万元"、"90日"、"0.005%"）
- 所有政策文件名称和文号（如"财会〔2017〕15号"）
- 所有判断条件（如"当……时才适用"）
- 所有 SOP 执行步骤的编号和核心动作
- 所有"特殊场景与例外"条目

# Input
<Oversized_Know_How>
{oversized_kh}
</Oversized_Know_How>

# Output Format
请严格按照以下 JSON 格式输出！不要包含任何额外的 Markdown 标记（如 ```json）。
**⚠️ 转义要求**：必须将 Markdown 文本中的所有换行符替换为 `\\n`，所有双引号替换为 `\\"。

{{
  "Shrink_Log": "简要说明删除/合并了哪些内容（例如：'删除了3处举例说明，合并了利息处理的2个重复条目，压缩政策依据为仅保留文号'）。",
  "Shrunken_Know_How": "（压缩后的完整 Markdown 文本，目标 {target_chars} 字以内）"
}}
"""


def merge_v0(existing=None, increment=None):
    existing_text = existing if existing and existing.strip() else "（当前为空，请将增量知识作为初始全局知识库直接输出，无需合并）"
    return f"""# Role
你是一位顶级的「知识库总架构师」与「主编」。你正在执行一项**累增式知识整合**任务：将一个新的【增量知识模块】与现有的【全局知识库】进行精密融合，产出一份更完整、更精炼、完全无冗余的【新版全局知识库】。

# Objective
将【增量知识模块】中的所有知识点**逐一审核**，并按以下规则处置：
1. **完全重叠** → 静默丢弃，不重复写入。
2. **部分重叠** → 与现有对应章节精密合并，保留两者的有效细节，消除冗余表述。
3. **全新内容** → 归入现有结构的最合适位置（若无合适位置则新建章节）。

# Strict Constraints (绝对不可违背的红线)
1. **禁止机械拼接**：绝不允许直接在【全局知识库】末尾追加【增量模块】。必须真正读懂两份文档，识别重叠、冲突与互补关系，再重新撰写输出。
2. **结构稳定性**：优先维护并扩展现有结构框架。只在增量内容确实无法归入任何现有章节时，才创建新的顶层章节，并保持全文章节编号连续一致。
3. **知识无损原则 (Zero Knowledge Loss)**：绝对不能丢失任何具体的判断阈值（如"≤100万元"、"90日"）、前提条件、政策文件编号或执行步骤。即使两条规则高度相似，也必须完整保留各自独特的细节差异。
4. **输出自洽性**：最终的【新版全局知识库】须是一份完全独立的文档，读者无需参考任何外部资料即可完整理解。
5. **禁止内容缩水**：合并后的文档在知识覆盖的广度与深度上，必须 ≥ 合并前两份文档内容的并集，严禁以"精炼"或"概括"为由删减具体条款或政策依据。

# Input Data
<Global_Know_How>
{existing_text}
</Global_Know_How>

<Incremental_Module>
{increment}
</Incremental_Module>

# Workflow (你的思考与执行步骤)
1. **结构解析**：分别梳理两份文档的章节骨架与核心主题。
2. **逐条比对**：将【增量模块】的每条知识点，在【全局知识库】中寻找对应位置：
   - 完全覆盖 → 标记"丢弃"
   - 部分重叠 → 标记"合并"，记录两者的差异点与互补信息
   - 无对应 → 标记"新增"，确定最佳归入章节
3. **重构撰写**：以"权威行业手册主编"的口吻，按现有结构框架重新撰写完整文档：
   - 将"合并"条目精密融入现有对应章节，消除重复表述的同时保留所有有效细节
   - 将"新增"条目有机纳入文档结构，确保风格统一、逻辑连贯

# Output Format
请严格按照以下 JSON 格式输出！不要包含任何额外的 Markdown 标记（如 ```json）。
**⚠️ 极其重要的转义警告**：必须将生成的 Markdown 文本中的所有换行符替换为 `\\n`，所有双引号替换为 `\\"，以确保最终输出的是合法的单层 JSON！

{{
  "Merge_Log": "简要说明本次合并操作（例如：'新增了贷款损失准备金章节；将税务处理相关内容并入现有「税务合规」章节，消除3处重叠；补充了2条政策文件编号'）。",
  "Merged_Know_How": "# 债权融资业务综合知识手册\\n\\n## 一、 宏观业务准则\\n……（完整的全文 Markdown，不得截断）……\\n\\n## N、 政策与合规依据汇总\\n……"
}}
"""